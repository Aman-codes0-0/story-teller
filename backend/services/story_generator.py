def generate_story_text(prompt, language):
    # This will be replaced by actual LLM call
    # response = openai.Completion.create(...)
    if language == "hindi":
        return "एक समय की बात है..."
    return "Once upon a time..."
